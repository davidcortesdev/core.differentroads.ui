/**
 * Constants for carousel configuration across the application
 */
export const CAROUSEL_CONFIG = {
  /**
   * Default autoplay interval in milliseconds for all carousels
   */
  DEFAULT_AUTOPLAY_INTERVAL: 7000,
};
